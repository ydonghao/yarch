# miniprogram/

小程序客户端预留位：规约已定稿 v1.0（2026-09-10，[../contract/clients/miniprogram.md](../contract/clients/miniprogram.md)）——原生 + TypeScript 唯一入册栈，消费 `stacks/` 产出的 API 契约（RestResponse 解包、错误码语义、X-Trace-Id 透传，经 `@yarch/contract` 注入 wx.request 适配器）。实现触发式：首个小程序工程创建时生长（登记 [../contract/registry.md](../contract/registry.md) 六节）。
