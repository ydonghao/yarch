# clients/ · 客户端预留位

按项目需要生长，不预先铺摊子。各目录只保留定位说明，有真实需求时再落实现。

| 目录 | 定位 |
|---|---|
| `mobile/` | uni-app / Flutter |
| `miniprogram/` | 小程序 |
| `desktop/` | Tauri（Rust + Web，兼 Rust 练手落点） |

接入时同样遵守 `contract/`：RestResponse 解包、错误码语义、X-Trace-Id 透传。
