<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.yuandonghao</groupId>
        <artifactId>yarch-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../../platform/yarch-parent</relativePath>
    </parent>
    <artifactId>yarch-auth-spring-boot-starter</artifactId>
    <description>认证机制件（E3/G4，账号模型归业务仓）：JWT(HS256 默认，Verifier SPI 可扩 RS/ES) + Bearer 解析 + 2xxx 映射 + @RequireRoles</description>
    <dependencies>
        <dependency>
            <groupId>io.github.yuandonghao</groupId>
            <artifactId>yarch-web-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.nimbusds</groupId>
            <artifactId>nimbus-jose-jwt</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.yuandonghao</groupId>
            <artifactId>yarch-test-spring-boot-starter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
