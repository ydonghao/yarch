<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.ydonghao</groupId>
        <artifactId>yarch-parent</artifactId>
        <version>0.1.0</version>
        <relativePath>../../platform/yarch-parent</relativePath>
    </parent>
    <artifactId>yarch-captcha-spring-boot-starter</artifactId>
    <name>yarch-captcha-spring-boot-starter</name>
    <description>验证码（E2）：图形码生成 + Redis 一次性 token（TTL）+ 校验；入口形态 GET /api/v1/captcha</description>
    <dependencies>
        <dependency>
            <groupId>io.github.ydonghao</groupId>
            <artifactId>yarch-web-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.ydonghao</groupId>
            <artifactId>yarch-redis-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.ydonghao</groupId>
            <artifactId>yarch-test-spring-boot-starter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
