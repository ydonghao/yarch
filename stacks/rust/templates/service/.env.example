# yarch rust 服务环境（复制为 .env 后改）
YARCH_SERVICE={{project-name}}
YARCH_ENV=local
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/{{crate_name}}?sslmode=disable
# 探活跳过建库迁移（连接惰性场景）
# YARCH_SKIP_MIGRATIONS=true
